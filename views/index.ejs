<!DOCTYPE html>
<html lang="sv">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kurslista</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

    <div class="container">
        <main class="main-container">
            <nav>
            <a href="/">Startsida</a>
            <a href="/addcourse">Lägg till kurs</a>
            <a href="/about">Om sidan</a>
        </nav>

            <h1>Kurslista</h1>
            <% if(rows.length > 0) { %>
                <% rows.forEach((course) => { %>
                    <article>
                        <p><strong>Kurskod: </strong><%= course.coursecode %></p>
                        <p><strong>Kursnamn: </strong><%= course.coursename %></p>
                        <p><strong>Progression: </strong><%= course.progression %></p>
                        <p><strong>Kursplan: </strong><%= course.syllabus %></p>
                    </article>
                <% }); %>
           <% } %>
        </main>
    </div>
</body>
</html>